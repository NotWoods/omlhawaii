{
	"body": "{{ content }}",
	"images": {
		{% for imghash in page.images.list %}
			{% unless forloop.first %},{% endunless %}"{{ page.images.folder }}{{ imghash[0] }}": "{{ imghash[1] }}"
		{% endfor %}
	}
}
